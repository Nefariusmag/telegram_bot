version: '3.3'
services:
  bot:
    env_file: .env
    build:
      context: .
      dockerfile: Dockerfile
    image: alert-telegram
    restart: always
    ports:
      - 5000:5000

networks:
  default: {}
